import containsDuplicate from './containsDuplicate';
import { listPrime } from './prime';

// Given an array of numbers, check whether this array contains duplicate numbers.
// Input: [2, 4, 6, 1, 74, 36, 6]
// Output: true
console.log(containsDuplicate([2, 4, 6, 1, 74, 36, 6]));

// Given a positive number n, count the number of prime numbers less than that.
// Input: n = 9
// Output: 4
console.log(listPrime(9));